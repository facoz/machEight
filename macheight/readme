There is the test with a file that contains units test for the function to find the pairs that match the target number, the main file that contains the functions is the called "find_pairs" that accept 2 inputs one with the numbers separated by comma and the target number that want to find the pairs
